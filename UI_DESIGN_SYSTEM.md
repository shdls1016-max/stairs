# STARBOUND STEPS UI 디자인 시스템

## 핵심 인상

모든 UI는 `귀엽고, 상큼하고, 말랑하고, 친근한 장난감`처럼 보여야 한다. 고급스럽거나 무거운 판타지보다 누르고 싶고 기분이 좋아지는 캐주얼 게임 감성을 우선한다.

## 공통 토큰

- 메인: 밝은 라벤더 `#AD9BEF`
- 연한 라벤더 `#C9BDF8`
- 크림 `#FFFAF0`
- 하늘색 `#A9E6F3`
- 민트 `#91DFC8`
- 따뜻한 노랑 `#FFD86B`
- 살구색 `#FFB39F`
- 기본 글자 `#58628A`
- 보조 글자 `#7B82A4`

실제 구현에서는 `app/globals.css`의 `--ui-*` 변수를 사용한다. 새 화면에서 임의의 진한 보라색이나 금색을 추가하지 않는다.

## 형태와 깊이

- 버튼과 작은 컨트롤은 충분히 둥근 젤리 형태로 만든다.
- 팝업은 크림색의 부드러운 면과 흰색 테두리를 사용한다.
- 깊이는 얇은 하단 받침과 흐린 그림자 한 겹으로만 표현한다.
- 금속 테두리, 두꺼운 외곽선, 무거운 그림자, 딱딱한 직선을 사용하지 않는다.
- 정보는 필요한 만큼만 묶고 웹사이트 카드처럼 여러 패널로 나누지 않는다.

## 버튼과 아이콘

- PLAY, 확인, 재시작, 일시정지, 좌우 조작은 동일한 밝기와 눌림 규칙을 공유한다.
- 기본 버튼은 밝은 라벤더, 보조 버튼은 크림색을 사용한다.
- 아이콘은 둥근 끝과 단순한 실루엣을 사용하며 작은 크기에서도 한눈에 보여야 한다.
- 누를 때 `살짝 축소 + 2px 아래 이동`, 가리킬 때 `약한 확대`로 반응한다.

## HUD와 피드백

- HUD는 밝고 반투명한 한 덩어리로 표현하고 진한 상태창처럼 만들지 않는다.
- 시간 바는 크림색 트랙과 노랑·살구·코랄의 상태색만 사용한다.
- 콤보는 패널 없이 노랑·살구색 타이포 자체로 표현한다.
- 캐릭터보다 UI 이펙트와 파티클이 더 눈에 띄어서는 안 된다.

## 팝업과 결과 화면

- 일시정지, 설정, 게임오버, 최고기록 화면은 같은 크림·라벤더 패널을 공유한다.
- 등장할 때 작은 크기에서 나타나 살짝 커졌다가 원래 크기로 돌아오는 젤리 모션을 사용한다.
- 제목은 둥글고 읽기 쉽게 유지하며 과한 외곽선이나 입체 텍스트를 사용하지 않는다.

## 금지 항목

- RPG·MMORPG·판타지·카드게임 UI
- 금속, 골드 테두리, 진한 남색 또는 진한 보라색 중심 팔레트
- 무거운 그림자와 과도한 입체감
- 대시보드형 카드 배열
- 화면마다 다른 버튼, 아이콘, 타이포 스타일

새 UI를 추가할 때는 새 디자인 언어를 만들지 말고 이 문서와 `--ui-*` 토큰을 먼저 재사용한다.
